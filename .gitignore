.firecrawl/
*.bak
research/apk/
research/unpacked/
work/

# compiled eval test binaries (sources are tracked)
eval/d-flip-exp
eval/test-weight-persist

# build directory (dist/ has the committed pre-built addon)
build/

# language model files (distributed via GitHub Releases)
*.lm
*.lm.predict
*.arpa

# dist/ is tracked (pre-built addon), but ignore anything else dropped in
dist/*
!dist/libomarime-state.so
!dist/omarime-state.conf

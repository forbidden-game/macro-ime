.firecrawl/
__pycache__/
*.pyc
*.bak
work/

# compiled eval test binaries (sources are tracked)
eval/test-weight-persist
eval/macro-ime-eval

# build directory (dist/ holds the committed pre-built addon)
build/

# language model files (distributed via GitHub Releases)
*.lm
*.lm.predict
*.arpa

# dist/ is tracked (pre-built addon + source sidecar)
dist/*
!dist/libmacro-ime-state.so
!dist/libmacro-ime-state.so.src
